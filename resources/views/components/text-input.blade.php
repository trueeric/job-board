<input type="text" name="{{ $name }}" id="{{ $name }}" placeholder="{{ $placeholder }}"
    value="{{ $value }}"
    class="w-full rounded-md py-1.5 px-2.5 text-sm ring-1 ring-slate-300 placeholder:text-slate-400 focus:ring-2" />
